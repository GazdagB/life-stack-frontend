# Browser-facing configuration. Never put secrets in VITE_* variables.
VITE_API_URL=/api

# Public legal-page identity. These values are embedded in the browser bundle; never put secrets here.
VITE_LEGAL_OPERATOR_NAME=LifeOS private operator
VITE_LEGAL_CONTACT_EMAIL=
